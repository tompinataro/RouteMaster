# 2026-02-24

- RouteMaster:
  - ASC was reset from DONE to READY when local IPA was missing.
  - Local IPA downloaded to `RouteMaster/artifacts/ios/routemaster-build-53.ipa`.
  - New ASC submission scheduled: `634f397f-2846-4aae-999f-0f435e32dd55`.
- Pool-Steward:
  - Local IPA downloaded to `Pool-Steward/artifacts/ios/pool-steward-build-1.ipa`.
  - ASC submission scheduled: `ee22a99a-16ed-4eaf-af2a-1ce676e58dcb`.
  - `asc_submission_status` set to `RUNNING`.
- Pull-Tab Valet:
  - iOS build is blocked in non-interactive mode due missing iOS signing credential setup for this app.
  - `build_ios_ipa_status` set to `BLOCKED`; `row_overall_status` set to `BLOCKED`.
- DVD Valet:
  - Created Expo scaffold at `DVD-Valet/mobile` and linked EAS project `@pinataro/dvd-valet` (`8800b34c-1af1-4f86-85d8-8ac9b532d7e3`).
  - iOS IPA build attempt failed in non-interactive mode due missing iOS signing credentials (requires one interactive credential setup).
  - Action Grid row remains `BLOCKED` for `build_ios_ipa_status`.
